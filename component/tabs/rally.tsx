import { SafeAreaView, Text} from 'react-native';

export default function RallyScreen(){
    return (
        <SafeAreaView style={{
            flex:1,
            justifyContent:'center',
            alignItems:'center'
        }}>
            <Text>대회공고당</Text>
        </SafeAreaView>
    );
}